apiVersion: v1
kind: Pod
metadata:
  name: single-nginx-pod
  labels:
    app: single-nginx
spec:
  containers:
    - name: nginx
      image: nginx:1.25-alpine
      ports:
        - containerPort: 80
