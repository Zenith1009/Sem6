## Assignment 2 - Network Measures

**Dataset: Karate club graph dataset will be utilized which is available in the network library.**

*Following tasks need to be performed:*

1. Calculate the degree of each node in the network
2. Plot the graph in such a way that size of a node is scaled according to their respective degree i.e.; the node having higher degree will be bigger in size as compared to a node having lower degree.[Hint: use the node_size attribute of the network.draw() function]
3. Plot the “Degree distribution” of the network
4. Plot the “Cumulative Degree distribution” of the network
5. Calculate local clustering coefficient (take one node as example node, see its connections
and verify local clustering coefficient obtained with formula)
6. Calculate Diameter, Density, and Average path length of the network.

**Deliverable:**
A single Jupyter Notebook file containing the code for all 6 tasks. Each task (1–6) should be clearly labeled using code comments or Markdown cells.
