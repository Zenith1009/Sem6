---
geometry: margin=0.75in
fontsize: 11pt
mainfont: JetBrains Mono
monofont: JetBrains Mono

header-includes:
  - \usepackage{xcolor}
  - \setlength{\parindent}{0pt}
  - \setlength{\parskip}{0.6em}
  - \definecolor{inlinebg}{HTML}{44475a}
  - \definecolor{inlinefg}{HTML}{f8f8f2}
  - \let\oldtexttt\texttt
  - \renewcommand{\texttt}[1]{\colorbox{inlinebg}{\oldtexttt{\textcolor{inlinefg}{#1}}}}
---

\begin{center}
{\Large\bfseries System Software}

\vspace{0.2em}

{\large Assignment 3}

\vspace{0.8em}

{\LARGE\bfseries Recursive Descent Parser (RDP)}

\vspace{1.2em}

\rule{0.65\textwidth}{0.5pt}

\vspace{1.2em}

{\large
\textbf{Student Name:} \textit{Naishadh Rana}\\[0.4em]
\textbf{Roll No:} U23CS014
}

\vspace{1.2em}

\rule{0.65\textwidth}{0.5pt}
\end{center}

\vspace{1.5em}


## 1. Implement Recursive Descent Parser (RDP) without backtracking for the given grammar.

| Non-Terminal | Production |
|--------------|------------|
| E | E + T \| T |
| T | T * F \| F |
| F | ( E ) \| id |

**Grammar Definition**

```md
E  → E + T | T
T  → T * F | F
F  → ( E ) | id
```

<u>**Code:**</u>

```c
#include <stdio.h>
#include <string.h>
#include <ctype.h>

char input[100];
int pos = 0;
int error = 0;

void E();
void E_prime();
void T();
void T_prime();
void F();

void skip_space() {
    while (input[pos] == ' ' || input[pos] == '\t')
        pos++;
}

void match(char c) {
    skip_space();
    if (input[pos] == c)
        pos++;
    else
        error = 1;
}

// E -> T E'
void E() {
    if (error) return;
    T();
    E_prime();
}

// E' -> + T E' | eps
void E_prime() {
    if (error) return;
    skip_space();
    if (input[pos] == '+') {
        pos++;
        T();
        E_prime();
    }
    // else epsilon, do nothing
}

// T -> F T'
void T() {
    if (error) return;
    F();
    T_prime();
}

// T' -> * F T' | epsilon
void T_prime() {
    if (error) return;
    skip_space();
    if (input[pos] == '*') {
        pos++;
        F();
        T_prime();
    }
    // else epsilon, do nothing
}

void F() {
    if (error) return;
    skip_space();
    if (input[pos] == '(') {
        pos++;
        E();
        match(')');
    }
    else if (isalpha(input[pos])) {
        while (isalnum(input[pos]))
            pos++;
    }
    else if (isdigit(input[pos])) {
        while (isdigit(input[pos]))
            pos++;
    }
    else {
        error = 1;
    }
}

int main() {
    printf("Enter expression: ");
    fgets(input, sizeof(input), stdin);
    input[strlen(input)-1] = '\0';  // remove newline
    
    printf("\nGrammar (after left recursion elimination):\n");
    printf("E  -> T E'\n");
    printf("E' -> + T E' | epsilon\n");
    printf("T  -> F T'\n");
    printf("T' -> * F T' | epsilon\n");
    printf("F  -> ( E ) | id\n\n");
    E();
    skip_space();
    printf("Result: ");
    if (!error && input[pos] == '\0')
        printf("VALID expression (Accepted)\n");
    else
        printf("INVALID expression (Rejected)\n");
    return 0;
}
```

\newpage

<u>**Output:**</u>

- Case 1: `a+b*c`
```bash
naish@MacBook-Air Lab3 % ./rdp
Enter expression: a+b*c

Grammar (after left recursion elimination):
E  -> T E'
E' -> + T E' | epsilon
T  -> F T'
T' -> * F T' | epsilon
F  -> ( E ) | id

Result: VALID expression (Accepted)
```
- Case 2: `a*b+c`
```bash
naish@MacBook-Air Lab3 % ./rdp
Enter expression: a*b+c

Grammar (after left recursion elimination):
E  -> T E'
E' -> + T E' | epsilon
T  -> F T'
T' -> * F T' | epsilon
F  -> ( E ) | id

Result: VALID expression (Accepted)
```
- Case 3: `a++b`
```bash
naish@MacBook-Air Lab3 % ./rdp                                       
Enter expression: a++b

Grammar (after left recursion elimination):
E  -> T E'
E' -> + T E' | epsilon
T  -> F T'
T' -> * F T' | epsilon
F  -> ( E ) | id

Result: INVALID expression (Rejected)
```

\newpage

- Case 4: `(a+b)*c`
```bash
naish@MacBook-Air Lab3 % ./rdp
Enter expression: (a+b)*c

Grammar (after left recursion elimination):
E  -> T E'
E' -> + T E' | epsilon
T  -> F T'
T' -> * F T' | epsilon
F  -> ( E ) | id

Result: VALID expression (Accepted)
```